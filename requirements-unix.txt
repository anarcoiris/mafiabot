# Additional requirements for Linux/Mac systems
# Install with: pip install -r requirements.txt -r requirements-unix.txt

# Production WSGI server (Linux only)
gunicorn>=23.0.0

# Optimized event loop (Linux/Mac only)
uvloop>=0.19.0
