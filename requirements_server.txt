# Server-specific requirements for Mafia Bot multiplayer server
# Install with: pip install -r requirements_server.txt

# WebSocket server
websockets>=12.0,<13.0

# JWT authentication
PyJWT>=2.8.0,<3.0

# Async utilities (already in main requirements but ensuring compatibility)
aiosqlite>=0.20.0
asyncio>=3.4.3

# High-performance event loop (optional but recommended for production)
uvloop>=0.19.0; platform_system != "Windows"

# For monitoring and admin features (optional)
prometheus-client>=0.19.0  # Metrics export
